# Password Generator Mini-Project (Experiments)
This repository contains a secure password generator implemented in Python, utilizing the `secrets` module for cryptographically strong random number generation and `shelve` for persistent password storage. The application is designed for creating and managing secure credentials locally.
